# Données collectées : elles vivent sur le serveur, jamais dans le dépôt
data/supporters.jsonl
data/ratelimit.json
data/*.json
data/*.jsonl

# Sources haute définition des images (non déployées)
assets/img/_source/

# Divers
.DS_Store
*.log
deploiement.conf

# Statistiques et sauvegardes de contenu (vivent sur le serveur)
data/stats-*.json
data/contenu-*.json
data/admin-tentatives.json
data/track-limite.json

# Secrets : jamais dans le dépôt
char2026-secrets/
*-secrets/
